# Cursor Ring & Magnifier v0.2.0

画面共有やセミナーで、カーソルの位置や細かい文字を見せやすくするmacOSアプリです。
これまでの機能と設定を維持しながら、描画・マウス監視・拡大鏡の取り込み処理を軽量化しました。

## ダウンロード
- 下の **Assets** から **`CursorRingMagnifier-macOS.zip`** をダウンロードしてください。
- 対象: **Apple Silicon搭載Mac / macOS 13以降**。今回の配布ZIPはIntel Mac用ではありません。
- `CursorRingMagnifier-macOS.zip.sha256` はダウンロードファイルの整合性確認用です。
- `Source code` は開発者向けのソースコードで、起動できるアプリは含まれていません。

## 今回の変更
- 常時の全画面再描画を廃止し、小さなリング専用ウィンドウを必要なときだけ更新。
- 拡大鏡をScreenCaptureKitによる非同期取り込みに変更。必要な範囲だけを最大30fpsで取得し、使わない間は停止。
- 高頻度のマウス移動でも、古い更新処理や画面画像が積み上がらない構造に変更。
- 設定の保存をまとめて処理し、自動起動の登録は対象設定を変更したときだけ実行。
- クリック中の色と縮小の維持、ショートカット、カスタマイズ、日本語設定、ログイン起動を継続。
- レンズサイズに応じた拡大率の計算、画面端や小さなディスプレイでの収まり、スリープ復帰を改善。
- スクリーンショットの範囲選択中も非表示を維持するよう調整。

## 確認状況
- 自動テスト27項目が合格。拡大率とサイズは全20組について円形/長方形を確認。
- ユーザーの実操作でリングと拡大鏡の基本表示を確認。
- 同じ開発機でのリングOFF時の短時間測定では、CPU使用率は旧版の約8〜11%から表示上0.0%に低下。これは待機時の観測値であり、Zoom共有中の性能やバッテリー消費を保証する値ではありません。

## インストール・更新
1. `CursorRingMagnifier-macOS.zip` をダウンロード
2. 起動中の旧版をメニューバーから終了
3. ZIPを解凍し、`CursorRingMagnifier.app` を「アプリケーション」フォルダに入れて旧版を置き換え
4. アプリを起動。保存済みの色・サイズ・ショートカットなどはそのまま引き継がれます

既定の操作は **Control + M** でリング表示の切り替え、**Control長押し** で拡大鏡です。以前にキーを変更している場合は、その設定が優先されます。

## 権限・配布上の注意
- クリックフィードバックとショートカットには「アクセシビリティ」、拡大鏡には「画面収録」の許可が必要です。更新後に再確認が求められた場合は、アプリの設定画面から権限をご確認ください。
- アドホック署名版であり、AppleのDeveloper ID署名・公証は未実施です。ダウンロード後の初回起動時にmacOSの警告が表示される場合があります。
- **Zoom参加者側の表示・録画と、共有中の負荷は引き続き実機確認が必要です。** 本番のセミナー前に、デスクトップ共有でお試しください。
- 外部の撮影ツールなどではリングが映り込む場合があります。確実に除外したい撮影では、あらかじめリングをOFFにしてください。
- アプリ自身は画面・音声・キー入力内容を保存・送信しません。
